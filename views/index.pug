include head.html
style
	include css/header.css
	include css/body.css
	include css/main-content.css
body
	include header.html
	include main-content.html
	#features
		if login
			h3 Search by title
			if erro
				span(
					style="color : red;"
				) Invalid Title
			form#search(
				action="/search"
				method="post"
			)
				input#title(
					type="text"
					name="searchTitle"
					required
				)
				button(
					type="submit"
				) Search
		.boxes
			#web
				if content[0] != null
					h2=content[0].title
					p=content[0].description
				else
					h2 Adicione algo !!
					p clique no link admin e adicione algum conteúdo !!
			#apis
				h2 APIs
				p Com uma miríade de métodos utilitários HTTP e middleware a seu dispor, criar uma API robusta é rápido e fácil.
			#performance
				h2 Desempenho
				p O Express fornece uma camada fina de recursos fundamentais para aplicativos da web, sem obscurecer os recursos do Node.js que você conhece e ama. 
			#frameworks
				h2 LoopBack
				p Desenvolva aplicativos orientado a modelos com um framework baseado em Express.Descubra mais em loopback.io.
	if login
		a(
			href="/admin"
		) admin

	a(
		href="/login"
	) login